local outline = require('outline')

outline.setup({
  outline_window = {
    auto_close = true,
  },
  symbol_folding = {
    autofold_depth = 1,
    auto_unfold = {
      hovered = true,
    },
  },
})
